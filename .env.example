# Copy this file to .env and fill in your own key.
# The key is read at build time via import.meta.env (Vite) and is required
# for the AI-driven simulation to call the model.
VITE_OPENAI_API_KEY=your-openai-api-key-here
